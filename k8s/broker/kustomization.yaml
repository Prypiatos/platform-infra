apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Argo CD can point at this folder and apply everything in the order below.
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - service-external.yaml
